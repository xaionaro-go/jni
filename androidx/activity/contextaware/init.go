// Code generated by javagen. DO NOT EDIT.

package contextaware

import (
	"fmt"
	"sync"
	"unsafe"

	"github.com/AndroidGoLab/jni"
)

// Ensure imports are used.
var (
	_ = fmt.Errorf
	_ = unsafe.Pointer(nil)
)

// jniTrue is the JNI representation of a true boolean value.
const jniTrue uint8 = 1

var (
	initOnce sync.Once
	initErr  error

	clsOnContextAvailableListener                   *jni.GlobalRef
	midOnContextAvailableListenerOnContextAvailable jni.MethodID
	midOnContextAvailableListenerToString           jni.MethodID

	clsContextAwareHelper                                 *jni.GlobalRef
	midContextAwareHelperCtor                             jni.MethodID
	midContextAwareHelperPeekAvailableContext             jni.MethodID
	midContextAwareHelperAddOnContextAvailableListener    jni.MethodID
	midContextAwareHelperRemoveOnContextAvailableListener jni.MethodID
	midContextAwareHelperDispatchOnContextAvailable       jni.MethodID
	midContextAwareHelperClearAvailableContext            jni.MethodID
	midContextAwareHelperToString                         jni.MethodID

	clsContextAware                                 *jni.GlobalRef
	midContextAwarePeekAvailableContext             jni.MethodID
	midContextAwareAddOnContextAvailableListener    jni.MethodID
	midContextAwareRemoveOnContextAvailableListener jni.MethodID
	midContextAwareToString                         jni.MethodID

	clsContextAwareKt         *jni.GlobalRef
	midContextAwareKtToString jni.MethodID
)

func ensureInit(env *jni.Env) error {
	initOnce.Do(func() {
		initErr = doInit(env)
	})
	return initErr
}

// Init resolves all JNI class and method references for this package.
// It is safe to call multiple times (uses sync.Once internally).
// Exported for E2E testing on Android.
func Init(env *jni.Env) error {
	return ensureInit(env)
}

func doInit(env *jni.Env) error {
	var c *jni.Class
	var err error

	c, err = env.FindClass("androidx/activity/contextaware/OnContextAvailableListener")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsOnContextAvailableListener = env.NewGlobalRef(&c.Object)

		midOnContextAvailableListenerOnContextAvailable, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsOnContextAvailableListener)), "onContextAvailable", "(Landroid/content/Context;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midOnContextAvailableListenerToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsOnContextAvailableListener)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	c, err = env.FindClass("androidx/activity/contextaware/ContextAwareHelper")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsContextAwareHelper = env.NewGlobalRef(&c.Object)
		midContextAwareHelperCtor, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAwareHelper)), "<init>", "()V")
		if err != nil {
			env.ExceptionClear()
		}

		midContextAwareHelperPeekAvailableContext, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAwareHelper)), "peekAvailableContext", "()Landroid/content/Context;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midContextAwareHelperAddOnContextAvailableListener, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAwareHelper)), "addOnContextAvailableListener", "(Landroidx/activity/contextaware/OnContextAvailableListener;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midContextAwareHelperRemoveOnContextAvailableListener, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAwareHelper)), "removeOnContextAvailableListener", "(Landroidx/activity/contextaware/OnContextAvailableListener;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midContextAwareHelperDispatchOnContextAvailable, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAwareHelper)), "dispatchOnContextAvailable", "(Landroid/content/Context;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midContextAwareHelperClearAvailableContext, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAwareHelper)), "clearAvailableContext", "()V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midContextAwareHelperToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAwareHelper)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	c, err = env.FindClass("androidx/activity/contextaware/ContextAware")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsContextAware = env.NewGlobalRef(&c.Object)

		midContextAwarePeekAvailableContext, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAware)), "peekAvailableContext", "()Landroid/content/Context;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midContextAwareAddOnContextAvailableListener, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAware)), "addOnContextAvailableListener", "(Landroidx/activity/contextaware/OnContextAvailableListener;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midContextAwareRemoveOnContextAvailableListener, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAware)), "removeOnContextAvailableListener", "(Landroidx/activity/contextaware/OnContextAvailableListener;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midContextAwareToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAware)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	c, err = env.FindClass("androidx/activity/contextaware/ContextAwareKt")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsContextAwareKt = env.NewGlobalRef(&c.Object)

		midContextAwareKtToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsContextAwareKt)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	return nil
}
