// Code generated by javagen. DO NOT EDIT.

package executor

import (
	"fmt"
	"sync"
	"unsafe"

	"github.com/AndroidGoLab/jni"
)

// Ensure imports are used.
var (
	_ = fmt.Errorf
	_ = unsafe.Pointer(nil)
)

// jniTrue is the JNI representation of a true boolean value.
const jniTrue uint8 = 1

var (
	initOnce sync.Once
	initErr  error

	clsDefaultTaskExecutor                 *jni.GlobalRef
	midDefaultTaskExecutorCtor             jni.MethodID
	midDefaultTaskExecutorExecuteOnDiskIO  jni.MethodID
	midDefaultTaskExecutorPostToMainThread jni.MethodID
	midDefaultTaskExecutorIsMainThread     jni.MethodID
	midDefaultTaskExecutorToString         jni.MethodID

	clsArchTaskExecutor                      *jni.GlobalRef
	midArchTaskExecutorSetDelegate           jni.MethodID
	midArchTaskExecutorExecuteOnDiskIO       jni.MethodID
	midArchTaskExecutorPostToMainThread      jni.MethodID
	midArchTaskExecutorToString              jni.MethodID
	midArchTaskExecutorGetInstance           jni.MethodID
	midArchTaskExecutorGetMainThreadExecutor jni.MethodID
	midArchTaskExecutorGetIOThreadExecutor   jni.MethodID
	midArchTaskExecutorIsMainThread          jni.MethodID

	clsTaskExecutor                    *jni.GlobalRef
	midTaskExecutorExecuteOnDiskIO     jni.MethodID
	midTaskExecutorPostToMainThread    jni.MethodID
	midTaskExecutorExecuteOnMainThread jni.MethodID
	midTaskExecutorIsMainThread        jni.MethodID
	midTaskExecutorToString            jni.MethodID
)

func ensureInit(env *jni.Env) error {
	initOnce.Do(func() {
		initErr = doInit(env)
	})
	return initErr
}

// Init resolves all JNI class and method references for this package.
// It is safe to call multiple times (uses sync.Once internally).
// Exported for E2E testing on Android.
func Init(env *jni.Env) error {
	return ensureInit(env)
}

func doInit(env *jni.Env) error {
	var c *jni.Class
	var err error

	c, err = env.FindClass("androidx/arch/core/executor/DefaultTaskExecutor")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsDefaultTaskExecutor = env.NewGlobalRef(&c.Object)
		midDefaultTaskExecutorCtor, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsDefaultTaskExecutor)), "<init>", "()V")
		if err != nil {
			env.ExceptionClear()
		}

		midDefaultTaskExecutorExecuteOnDiskIO, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsDefaultTaskExecutor)), "executeOnDiskIO", "(Ljava/lang/Runnable;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midDefaultTaskExecutorPostToMainThread, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsDefaultTaskExecutor)), "postToMainThread", "(Ljava/lang/Runnable;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midDefaultTaskExecutorIsMainThread, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsDefaultTaskExecutor)), "isMainThread", "()Z")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midDefaultTaskExecutorToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsDefaultTaskExecutor)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	c, err = env.FindClass("androidx/arch/core/executor/ArchTaskExecutor")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsArchTaskExecutor = env.NewGlobalRef(&c.Object)

		midArchTaskExecutorSetDelegate, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsArchTaskExecutor)), "setDelegate", "(Landroidx/arch/core/executor/TaskExecutor;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midArchTaskExecutorExecuteOnDiskIO, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsArchTaskExecutor)), "executeOnDiskIO", "(Ljava/lang/Runnable;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midArchTaskExecutorPostToMainThread, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsArchTaskExecutor)), "postToMainThread", "(Ljava/lang/Runnable;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midArchTaskExecutorToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsArchTaskExecutor)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midArchTaskExecutorGetInstance, err = env.GetStaticMethodID((*jni.Class)(unsafe.Pointer(clsArchTaskExecutor)), "getInstance", "()Landroidx/arch/core/executor/ArchTaskExecutor;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midArchTaskExecutorGetMainThreadExecutor, err = env.GetStaticMethodID((*jni.Class)(unsafe.Pointer(clsArchTaskExecutor)), "getMainThreadExecutor", "()Ljava/util/concurrent/Executor;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midArchTaskExecutorGetIOThreadExecutor, err = env.GetStaticMethodID((*jni.Class)(unsafe.Pointer(clsArchTaskExecutor)), "getIOThreadExecutor", "()Ljava/util/concurrent/Executor;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midArchTaskExecutorIsMainThread, err = env.GetStaticMethodID((*jni.Class)(unsafe.Pointer(clsArchTaskExecutor)), "isMainThread", "()Z")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	c, err = env.FindClass("androidx/arch/core/executor/TaskExecutor")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsTaskExecutor = env.NewGlobalRef(&c.Object)

		midTaskExecutorExecuteOnDiskIO, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsTaskExecutor)), "executeOnDiskIO", "(Ljava/lang/Runnable;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midTaskExecutorPostToMainThread, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsTaskExecutor)), "postToMainThread", "(Ljava/lang/Runnable;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midTaskExecutorExecuteOnMainThread, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsTaskExecutor)), "executeOnMainThread", "(Ljava/lang/Runnable;)V")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midTaskExecutorIsMainThread, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsTaskExecutor)), "isMainThread", "()Z")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midTaskExecutorToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsTaskExecutor)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	return nil
}
