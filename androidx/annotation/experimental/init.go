// Code generated by javagen. DO NOT EDIT.

package experimental

import (
	"fmt"
	"sync"
	"unsafe"

	"github.com/AndroidGoLab/jni"
)

// Ensure imports are used.
var (
	_ = fmt.Errorf
	_ = unsafe.Pointer(nil)
)

// jniTrue is the JNI representation of a true boolean value.
const jniTrue uint8 = 1

var (
	initOnce sync.Once
	initErr  error

	clsUseExperimental            *jni.GlobalRef
	midUseExperimentalMarkerClass jni.MethodID
	midUseExperimentalToString    jni.MethodID

	clsExperimental         *jni.GlobalRef
	midExperimentalLevel    jni.MethodID
	midExperimentalToString jni.MethodID

	clsLevel         *jni.GlobalRef
	midLevelToString jni.MethodID
	midLevelValues   jni.MethodID
	midLevelValueOf  jni.MethodID
)

func ensureInit(env *jni.Env) error {
	initOnce.Do(func() {
		initErr = doInit(env)
	})
	return initErr
}

// Init resolves all JNI class and method references for this package.
// It is safe to call multiple times (uses sync.Once internally).
// Exported for E2E testing on Android.
func Init(env *jni.Env) error {
	return ensureInit(env)
}

func doInit(env *jni.Env) error {
	var c *jni.Class
	var err error

	c, err = env.FindClass("androidx/annotation/experimental/UseExperimental")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsUseExperimental = env.NewGlobalRef(&c.Object)

		midUseExperimentalMarkerClass, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsUseExperimental)), "markerClass", "()Ljava/lang/Class;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midUseExperimentalToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsUseExperimental)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	c, err = env.FindClass("androidx/annotation/experimental/Experimental")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsExperimental = env.NewGlobalRef(&c.Object)

		midExperimentalLevel, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsExperimental)), "level", "()Landroidx/annotation/experimental/Experimental$Level;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midExperimentalToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsExperimental)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	c, err = env.FindClass("androidx/annotation/experimental/Experimental$Level")
	if err != nil {
		// Class may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	} else {
		clsLevel = env.NewGlobalRef(&c.Object)

		midLevelToString, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsLevel)), "toString", "()Ljava/lang/String;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midLevelValues, err = env.GetStaticMethodID((*jni.Class)(unsafe.Pointer(clsLevel)), "values", "()[Landroidx/annotation/experimental/Experimental$Level;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

		midLevelValueOf, err = env.GetStaticMethodID((*jni.Class)(unsafe.Pointer(clsLevel)), "valueOf", "(Ljava/lang/String;)Landroidx/annotation/experimental/Experimental$Level;")
		if err != nil {
			// Method may not exist on this device's API level; skip and
			// report at invocation time instead of failing the entire init.
			env.ExceptionClear()
		}

	}

	return nil
}
