// Code generated by javagen. DO NOT EDIT.
// (manually patched in cycle 9: the generator-emitted shim used
// `RecyclerView$ViewHolder` in Java source positions where javac requires
// the dotted form, declared a no-arg-style constructor that violates
// RecyclerView.ViewHolder's `super(View)` requirement, and emitted
// @Override on methods that aren't actually overridable. The patched
// shim is a minimal concrete subclass that takes a View — it carries no
// dispatch logic; ViewHolder identity (the itemView field) is sufficient
// for our adapter use-case. Fix the generator to emit this shape directly.)

package center.dx.jni.generated;

import android.view.View;
import androidx.recyclerview.widget.RecyclerView;

public class RecyclerView$ViewHolderAdapter extends RecyclerView.ViewHolder {
    public RecyclerView$ViewHolderAdapter(View itemView) {
        super(itemView);
    }
}
